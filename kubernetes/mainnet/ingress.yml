---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: casperholders-tls
spec:
  entryPoints:
    - secure
  routes:
    - match: Host(`casperholders.com`)
      kind: Rule
      services:
        - name: casperholders-service
          port: 80
  tls:
    secretName: casperholders-cert
---
apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: casperholders-http
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`casperholders.com`)
      middlewares:
        - name: https-only
      kind: Rule
      services:
        - name: casperholders-service
          port: 80